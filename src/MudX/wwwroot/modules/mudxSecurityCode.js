
export function init(dotNetObjRef,container){if(!container)return;const inputs=container.querySelectorAll("input");inputs.forEach((input)=>{if(!input._pasteHandler){input._pasteHandler=(event)=>handlePaste(event,input,dotNetObjRef);input.addEventListener("paste",input._pasteHandler);}});}
export function cleanup(container){if(!container)return;const inputs=container.querySelectorAll("input");inputs.forEach((input)=>{if(input._pasteHandler){input.removeEventListener("paste",input._pasteHandler);delete input._pasteHandler;}});}
function handlePaste(event,input,dotNetObjRef){if(!event||!input||!dotNetObjRef)return;event.preventDefault();const paste=(event.clipboardData||window.clipboardData)?.getData("Text");if(paste){dotNetObjRef.invokeMethodAsync("ClipboardPasteEvent",input.id,paste);input.blur();}}
export function focusBlock(container,inputId){if(!container||!inputId)return;const input=container.querySelector("#"+inputId);if(input){try{input.focus();input.select();}
catch{}}}
export function focusNextAfterContainer(container){if(!container)return;setTimeout(()=>focusNextElement(),0);}
function focusNextElement(){const focusableSelector='a:not([disabled]), button:not([disabled]), input:not([disabled]), textarea:not([disabled]), select:not([disabled]), [tabindex]:not([disabled]):not([tabindex="-1"])';if(!document.activeElement)return;const container=document.activeElement.form||document;const focusableElements=Array.from(container.querySelectorAll(focusableSelector)).filter(element=>{return element.offsetWidth>0||element.offsetHeight>0||element===document.activeElement;});const currentIndex=focusableElements.indexOf(document.activeElement);const nextIndex=currentIndex+1;if(nextIndex<focusableElements.length){focusableElements[nextIndex].focus();try{focusableElements[nextIndex].select();}
catch{}}}